import React from 'react';
import classes from './Footer.module.css';

const footer = (props) => (
    <footer className={classes.Footer}>
        <p style={{'textAlign':'center','paddingTop':'10px' ,'color':'white'}}>Decentralized Gov LLC</p>
    </footer>
)

export default footer;
